# @iweioo/sdk

This private workspace package is the TypeScript SDK incubation boundary. The
checked-in OpenAPI document remains authoritative; generated types can be
reproduced with `npm run generate:sdk` from the repository root.

Runtime authentication, retries, idempotency, and tracing helpers will be added
in later stages only after their behavior is covered by the platform contract.
