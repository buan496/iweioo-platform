# Deployment adapters

This directory contains local and future production deployment adapters. The
Stage 2 Keycloak, Mailpit, PostgreSQL, and Redis identity profile is
deliberately local-only. The single-server, staging, and production profiles
remain deferred until identity, databases, secrets, backups, edge routing, and
observability can be configured together.
